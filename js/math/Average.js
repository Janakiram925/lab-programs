/**To compute the average of three given numbers. */
var a = 10, b = 20, c = 30;
var res = 0;

/**Average of n numbers is adding of number then dividing by n */
res = (a+b+c)/3;
console.log("The average of given 3 numbers is: "+res);